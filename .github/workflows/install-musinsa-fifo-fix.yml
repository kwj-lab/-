name: Legacy Musinsa FIFO Fix (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy FIFO installer is disabled
        run: |
          echo "Disabled: the old workflow-wide FIFO caused scheduled observations to drift and miss their intended slots."
          echo "Current policy: observations run on their scheduled slot; only repository writes are serialized."
